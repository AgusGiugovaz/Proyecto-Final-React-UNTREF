import { createContext } from "react";
//HomeContext que el profe lo llamó CotizadorContext (porque tiene todo el form)
//La idea de este es controlar los datos que tenga adentro del form
//Controlar los datos: Hacer la validacion del form, tener el precio estimado
const FormularioContext = createContext({});

export default FormularioContext;